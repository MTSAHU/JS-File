// while loop //

let index = 0;
while (index <= 10) {
    // console.log(`value of index is ${index}`);
    index = index + 2
}

let myArray = ["flash", "batman", "superman"]

let arr = 0  // there arr is zero ,(arr < myArray.length) = arr length should be smaller than myArray if you use <= 
while (arr < myArray.length) {
    console.log(`value is ${myArray[arr]}`);
    arr = arr + 1
}

let score = 11

do {
    // console.log(`Score is ${score}`);
    score++

} while (score <= 10);

// ** imp ** Array index start from 0 and Array length start from 1 //